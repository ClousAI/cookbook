{
  "_comment": "Drop this into Claude Desktop (claude_desktop_config.json), Cursor (.cursor/mcp.json), or any MCP client. Replace clous_live_... with your key. The server runs via npx; no install needed.",
  "mcpServers": {
    "clous": {
      "command": "npx",
      "args": ["-y", "@clousai/mcp"],
      "env": { "CLOUS_API_KEY": "clous_live_..." }
    }
  }
}
